# Per-module POM overrides (merged over the root POM_* metadata by Vanniktech).
# artifactId defaults to the Gradle project name (lustro-noop).
POM_NAME=Lustro No-op
POM_DESCRIPTION=Release-safe no-op artifact for Lustro: same public facades as the runtime, every body a no-op.
