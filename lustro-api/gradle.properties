# Per-module POM overrides (merged over the root POM_* metadata by Vanniktech).
# artifactId defaults to the Gradle project name (lustro-api).
POM_NAME=Lustro API
POM_DESCRIPTION=Public SPI for Lustro: the frozen contract types shared by the runtime and no-op artifacts.
