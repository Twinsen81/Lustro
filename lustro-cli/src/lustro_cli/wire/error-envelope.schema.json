{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://twinsen81.github.io/Lustro/wire-protocol/v1/error-envelope.schema.json",
  "title": "Error envelope",
  "description": "Uniform error body returned by every /api/v1/* route on failure.",
  "type": "object",
  "required": ["error", "message"],
  "properties": {
    "error": {
      "type": "string",
      "enum": [
        "bad_request",
        "unauthorized",
        "forbidden",
        "not_found",
        "method_not_allowed",
        "payload_too_large",
        "internal_error",
        "unavailable",
        "timeout"
      ],
      "description": "Short, stable, machine-readable error identifier (e.g. \"not_found\")."
    },
    "message": {
      "type": "string",
      "description": "Human-readable explanation of what went wrong."
    },
    "code": {
      "type": "string",
      "description": "Optional finer-grained error code for programmatic handling."
    },
    "field": {
      "type": "string",
      "description": "Optional name of the request field that caused the error."
    },
    "hint": {
      "type": "string",
      "description": "Optional actionable suggestion for resolving the error."
    }
  },
  "additionalProperties": false
}
